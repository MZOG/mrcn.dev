import * as React from "react"

import Container from "./container"
// import { Link } from "gatsby"

const Header = () => (
  <header className="py-5">
    <Container customClass="flex justify-between items-center"></Container>
  </header>
)

export default Header
